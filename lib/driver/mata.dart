class Mata{
  late String driveruid ,endpoint,parentname,parentphone,parentuid, requiredseats,
      startingpoint,key;

  Mata(this.driveruid,this.endpoint,this.parentname,this.parentphone,this.parentuid,this.requiredseats,
      this.startingpoint,this.key);
}